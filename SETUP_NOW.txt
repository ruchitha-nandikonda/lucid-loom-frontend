═══════════════════════════════════════════════════════
  QUICK SETUP: OpenAI API Key
═══════════════════════════════════════════════════════

CURRENT STATUS: API Key not configured

TO FIX (3 steps):

1. Get API key:
   → https://platform.openai.com/api-keys
   → Sign in → Create new secret key
   → Copy the key (starts with 'sk-')

2. Set the key:
   Run: ./setup-api-key.sh
   OR edit: dream-backend/.env
   Change: OPENAI_API_KEY=your_openai_api_key_here
   To:     OPENAI_API_KEY=sk-your-actual-key-here

3. Restart backend:
   → Stop server (Ctrl+C in uvicorn terminal)
   → Start again: uvicorn main:app --reload

THEN: Refresh browser and try "Transform Your Dream" again!

═══════════════════════════════════════════════════════
